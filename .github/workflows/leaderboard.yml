name: Leaderboard rebuild (manual only)

# Scheduled runs were stopped after the 2026 tournament finished.
# Left runnable on demand via the Actions tab or a workflow_dispatch API call.
on:
  workflow_dispatch: {}

permissions:
  contents: write

jobs:
  rebuild:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Rebuild leaderboard.html
        env:
          FOOTBALL_API_KEY: ${{ secrets.FOOTBALL_API_KEY }}
        run: python build_leaderboard_action.py
      - name: Commit if changed
        run: |
          git config user.name "sweepstake-bot"
          git config user.email "actions@github.com"
          for f in leaderboard.html scores.json build-status.json knockouts.json; do
            [ -f "$f" ] && git add "$f"
          done
          if git diff --cached --quiet; then
            echo "No changes to commit"
          else
            git commit -m "Leaderboard rebuild [skip ci]"
            git push
          fi
